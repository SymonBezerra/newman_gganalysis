# A Generative Approach to Ontologies for Machine Translation

This repository contains the source code for the paper whose title is mentioned above.

You can repeat the experiment by installing the `requirements.txt` on a Python environment (used version: 3.14.2).

The source text is in `source/newman.md`, and the results are in the `results` folder, in both XML files.